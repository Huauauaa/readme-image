{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "NODE_OPTIONS=--openssl-legacy-provider next dev",
    "build": "NODE_OPTIONS=--openssl-legacy-provider next build",
    "check:shot": "node src/shot/timeline.check.mjs"
  },
  "dependencies": {
    "bootstrap": "4.6.0",
    "next": "10.x",
    "react": "17.x",
    "react-bootstrap": "^1.6.1",
    "react-dom": "17.x",
    "three": "0.158.0"
  }
}
